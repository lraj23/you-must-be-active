{
	"name": "you-must-be-active",
	"version": "0.1.0",
	"description": "Stay active in #you-must-be-active or get kicked out",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"start": "node --env-file=.env app.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"license": "MIT",
	"dependencies": {
		"@slack/bolt": "^4.2.0"
	}
}